import fs from 'fs';
// We don't have canvas in node environment easily without installing it.
// I will just write the function logic conceptually to test it, or we can just implement it directly in omr.ts
